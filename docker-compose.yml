services:
    search-engine:
        build: .
        ports:
            - 6000:5002
        restart: unless-stopped
        networks:
            - nfdi-search-engine_default
networks:
    nfdi-search-engine_default:
        external: true
