services:
    search-engine:
        build: .
        ports:
            - 6000:8000
        restart: unless-stopped
        networks:
            - demo_network
networks:
    demo_network:
        external: true
