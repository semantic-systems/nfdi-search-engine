services:
    search-engine:
        build: .
        ports:
            - 6001:8000
        restart: unless-stopped
        networks:
            - demo_network
networks:
    demo_network:
        external: true
